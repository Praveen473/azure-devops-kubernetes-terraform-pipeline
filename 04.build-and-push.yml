pool:
  name: Default 

steps:
- task: Docker@2
  displayName: Build and Push Docker Image
  inputs:
    containerRegistry: 'praveen-docker-hub'
    repository: 'testpassword1234567890/currency-exchange-devops'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: |
      $(Build.BuildId)
